'use strict';

describe('Service: favorRepo', function () {

  // load the service's module
  beforeEach(module('devApp'));

  // instantiate service
  var favorRepo;
  beforeEach(inject(function (_favorRepo_) {
    favorRepo = _favorRepo_;
  }));

  // it('should do something', function () {
  //   expect(!!favorRepo).toBe(true);
  // });

});
